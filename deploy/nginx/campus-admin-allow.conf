# Django Admin 授信来源片段模板。由 provision-temporary-public.sh 渲染 __ADMIN_IP__ 后
# 安装到 /etc/nginx/snippets/campus-admin-allow.conf。仅允许授信管理 IP 与 loopback。
allow __ADMIN_IP__;
allow 127.0.0.1;
allow ::1;
deny all;
